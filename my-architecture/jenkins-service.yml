apiVersion: v1

kind: Service

metadata:
  name: jenkins-service
  labels:
    name: jenkins-service
    type: jenkins-service

spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    name: jenkins-pod
    type: jenkins-pod